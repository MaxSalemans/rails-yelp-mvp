
<h1>Write review</h1>

<%= simple_form_for ([@restaurant, @review]) do |f| %>
  <%= f.input :content %>
  <%= f.input :rating, collection: [0, 1, 2, 3, 4, 5] %>
  <%= f.submit "add new review", class: "btn btn-outline-primary" %>
<% end %>
